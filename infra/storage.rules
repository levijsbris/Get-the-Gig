rules_version = '2';
// Phase 0 placeholder: deny-by-default. Real rules land in Phase 3.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
